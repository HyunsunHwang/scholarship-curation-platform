import fs from "node:fs";
import path from "node:path";
import { fileURLToPath } from "node:url";
import { execFileSync } from "node:child_process";
import { buildPhase4FullCaptureDryRun } from "../lib/crawler-engine/runtime-diagnostics/phase4-full-capture-dry-run.mjs";
const root = path.resolve(path.dirname(fileURLToPath(import.meta.url)), ".."); const privateRoot = path.join(root, ".tmp", "phase4-private-artifacts"); const runIdentity = "phase4-full-dry-run-20260727";
const git = (args) => execFileSync("git", ["-C", root, ...args], { encoding: "utf8" }).trim(); const implementationCodeSha = git(["rev-parse", "HEAD"]); if (!/^[a-f0-9]{40}$/i.test(implementationCodeSha) || git(["status", "--porcelain", "--untracked-files=no"]) || execFileSync("git", ["-C", root, "diff", "--check"], { encoding: "utf8" }).trim()) throw new Error("phase4_dry_run_dirty_worktree");
const result = { ...buildPhase4FullCaptureDryRun({ repositoryRoot: root, privateArtifactRoot: privateRoot, checkpoint: path.join(privateRoot, "phase4-capture", runIdentity, "checkpoint.json"), readinessReportPath: path.join(root, "reports/runtime-diagnostics/phase4c-readiness-closeout-2026-07-27-run2.json"), runIdentity }), implementation_code_sha: implementationCodeSha, implementation_commit_message: git(["log", "-1", "--pretty=%s"]), report_generated_at: new Date().toISOString(), working_tree_clean: true, git_diff_check_passed: true, repository_branch: git(["branch", "--show-current"]), live_execution_released: false };
const report = path.join(root, "reports/runtime-diagnostics/phase4-full-capture-dry-run-2026-07-27-run2.json"); fs.writeFileSync(report, `${JSON.stringify(result, null, 2)}\n`, { flag: "wx" }); fs.writeFileSync(path.join(root, "reports/runtime-diagnostics/phase4-full-capture-dry-run-2026-07-27-run2.md"), `# Phase 4 full-capture dry run\n\n- Implementation SHA: ${implementationCodeSha}\n- Targets: ${result.target_inventory.target_source_count}\n- Network fetches: 0\n- Ready to execute: ${result.ready_to_execute}\n- Live release: disabled\n`, { flag: "wx" });
