# Post-Phase M Controlled Pilot

Post-Phase M operates the Post-Phase L normalized graph, append-only review, legacy compatibility, and hidden projection preview in the authorized non-production project only.

Two bounded cycles used the exact cohort `cau_001`, `cau_002`, `yonsei_060`, `cau_003`, `cau_007`, and `cau_008`, with at most 30 observed items, five pages, and source concurrency one. `cau_001`, `cau_003`, and `cau_007` produced repeatable attributable list-to-detail evidence. `cau_002` remained transport-blocked. `cau_008` and `yonsei_060` remained observed zero-match states. No outcome was inferred from missing output.

The expansion decision is GO for continued non-production operation because two expansion sources produced attributable evidence in both cycles. This decision does not authorize production operation or automatic publication.

The review lifecycle exercised `needs_review` then `approve` for `cau_003` and `cau_007`. Events remain append-only, effective decisions point to the latest event, and previews remain hidden with zero public leakage.

Runtime details are in `reports/post-phase-m-runtime-verification.json`, `reports/post-phase-m-source-health.json`, and the immutable cycle directories under `reports/post-phase-m-live`.
