# Production site URL (OG/metadata metadataBase). Safe to commit — not a secret.
# Also set in Vercel → Settings → Environment Variables if you override per environment.
NEXT_PUBLIC_SITE_URL=https://kunnect.xyz
